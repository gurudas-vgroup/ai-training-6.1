{
  "name": "shopify-ops-agent",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Admin-scoped Shopify operations agent — read-only inventory lookup plus an approval-gated order-status write, built on the Claude Agent SDK.",
  "scripts": {
    "start": "node src/cli.js",
    "test": "node --test \"test/*.test.js\""
  },
  "dependencies": {
    "@anthropic-ai/claude-agent-sdk": "^0.3.274",
    "dotenv": "^17.4.2",
    "zod": "^4.6.5"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
